# ios/Podfile
platform :ios, '15.0'

# Disable CocoaPods analytics
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

use_frameworks!
use_modular_headers!

target 'Runner' do
  # Flutter Pods
  pod 'Flutter', :path => File.join('Flutter', 'Flutter.framework')
end
